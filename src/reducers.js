const initialState = {
    people: [{id: 1, name: 'Toran'}, {id: 2, name: 'Jarrod'}, {id: 3, name: 'Brandon'}]
};

export default function(state=initialState, action) {
    // no actions yet ... just return state
    return state;
};
